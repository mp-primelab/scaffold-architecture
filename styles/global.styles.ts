import { css } from '@emotion/react';

const styles = {
  container: css`
    padding: 0;
  `,
  main: css`
    height: 100vh;
    display: grid;
    place-items: center;
  `,
};

export default styles;
