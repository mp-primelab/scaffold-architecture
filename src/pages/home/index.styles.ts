import { css } from '@emotion/react';

const styles = {
  title: css`
    margin: 0;
    line-height: 1.15;
    font-size: 4rem;
  `,
  main: css`
    grid-template-rows: 6rem 1fr;
  `,
};

export default styles;
